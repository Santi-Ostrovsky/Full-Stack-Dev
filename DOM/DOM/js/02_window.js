/*
WINDOW ==> Objeto contenedor de todos los elementos en el front (hereda propiedades de event target).

METODOS DE WINDOW (window.[...] ==> al ser el objeto global, no es necesario escribir 'window' pero puede hacerse igual).

    - open() ==> abre una ventana
    - close() ==> cierra una ventana
    - closed ==> retorna true/false si la ventana esta cerrada
    - stop() ==> detiene la carga del contenido en el sitio
    - alert() ==> mensaje de alerta
    - print() ==> abre el cuadro de dialogo para imprimir el documento
    - prompt() ==> solicita un input por parte del usuario
    - confirm() ==> solicita una confirmación por parte del usuario, y retorna true/false dependiendo de tal confirmación.

    - screen ==> devuelve una referencia al objeto de pantalla asociado con la ventana
    - screenLeft ==> devuelve la distancia horizontal entre el borde izquierdo del navegador y el de la pantalla (idem screenRight)
    - screenTop ==> devuelve la distancia vertical entre el borde superior del navegador y el de la pantalla
    - scrollX ==> devuelve la cant. de pixeles que el documento se desplaza de manera horizontal.
    - scrollY ==> devuelve la cant. de pixeles que el documento se desplaza de manera vertical.
    - scroll([x], [y]) ==> desplaza la ventana a un punto especifico de la ventana

    - resizeBy() ==> cambia el tamaño de la ventana en la cantidad especificada
    - resizeTo() ==> re-dimensiona la ventana dinámicamente
    - moveBy() ==> mueve la ventana en una cantidad relativa
    - moveTO() ==> mueve la ventana a una posición absoluta

--------------------------------------

LOCATION (pathName, protocolo, URL, etc.)

https://developer.mozilla.org/es/docs/Web/API/Location

--------------------------------------



--------------------------------------


*/
